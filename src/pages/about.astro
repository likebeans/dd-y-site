---
import { profile } from "@/data/profile";
import BaseLayout from "@/layouts/BaseLayout.astro";
---

<BaseLayout title="关于 - dd-y / 余非凡" description="关于余非凡：大模型算法工程师，关注从模型到系统。">
  <section class="about-page page-shell">
    <header>
      <p data-i18n-zh="关于" data-i18n-en="About">关于</p>
      <h1 data-i18n-zh="关于" data-i18n-en="ABOUT">关于</h1>
    </header>

    <div class="about-page__body">
      <aside>
        <span data-i18n-zh={profile.availability} data-i18n-en="AVAILABLE FOR GOOD WORK">{profile.availability}</span>
        <span>{profile.timezone}</span>
        <span data-i18n-zh="中国，北京" data-i18n-en="BEIJING, CHINA">{profile.location}</span>
      </aside>

      <div>
        <p>
          我是余非凡，一名大模型算法工程师。我关注的不是让模型完成一次漂亮的回答，而是如何把模型能力、上下文、检索、工具和业务流程组合成可以上线、可以评测、可以维护的 AI 系统。
        </p>
        <p>
          我的工作路径从模型、检索和推理服务开始，逐渐进入 RAG、Agent 与企业 AI 应用。越接近真实场景，我越意识到真正决定 AI 产品质量的，往往是数据结构、权限边界、流程约束、评测体系和工程可观测性。
        </p>
        <p>
          这个网站会长期记录我的项目、文章、工作经历和一些短思考。它既是求职展示入口，也是一处整理判断力的个人空间。
        </p>
        <p>
          后续我也会把一些可复用的工程实践、评测脚本和工具封装整理成开源项目，让这个空间不只展示结果，也展示持续打磨系统能力的方法。
        </p>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .about-page {
    padding-top: clamp(3rem, 7vw, 6rem);
  }

  header {
    margin-bottom: clamp(2.5rem, 6vw, 5rem);
  }

  header p,
  aside {
    color: var(--color-muted);
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.02em;
    text-transform: uppercase;
  }

  h1 {
    margin: 1rem 0 0;
    font-family: var(--font-display);
    font-size: clamp(4.5rem, 12vw, 11rem);
    font-weight: 850;
    line-height: 0.82;
  }

  .about-page__body {
    display: grid;
    grid-template-columns: minmax(10rem, 0.25fr) minmax(0, 0.75fr);
    gap: clamp(2rem, 6vw, 6rem);
    border-top: 1px solid var(--color-line);
    padding-top: 2rem;
  }

  aside {
    display: grid;
    align-content: start;
    gap: 0.6rem;
  }

  .about-page__body div {
    max-width: 58rem;
  }

  p {
    margin: 0 0 1.4rem;
    font-size: clamp(1.12rem, 1.8vw, 1.48rem);
    line-height: 1.8;
  }

  @media (max-width: 760px) {
    .about-page__body {
      grid-template-columns: 1fr;
    }
  }
</style>
